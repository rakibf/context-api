// import createContext from "../lib/Context";
// export const ThemeContext = createContext();

// Replacing our own context with React context
import React from 'react';
export const ThemeContext = React.createContext();